import { makeMarbleMaze } from './marble-maze.js';

export default makeMarbleMaze({
  id: 'marble-maze-raised',
  name: 'Marble maze (raised)',
  description: 'The marble maze with its floor on the glass and the walls standing out of the screen. Tilt to roll the marble to the copper ring without dropping it into a pit. Requires motion sensors, in any tracking mode.',
  raised: true,
});
